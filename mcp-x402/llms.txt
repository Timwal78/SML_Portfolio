ScriptMasterLabs x402: ALL paid calls **$0.001** — primary **USDC on Base** (eip155:8453). Also accepts **USDG on Robinhood Chain** (eip155:4663) via X-PAYMENT-TX. Volume war. No API key.

# mcp-x402 — ScriptMasterLabs Autonomous Payment MCP Server
# MCP server for agents with autonomous USDC/RLUSD payments
# x402 pay-per-call · 85+ trading + federal tools · no API keys

> The first MCP server that pays for itself.

## What this is

mcp-x402 is the front door to ScriptMasterLabs agent data rails. AI agents (Claude, Cursor, GPT, etc.) connect once and get trading + federal/public-data tools with **x402 pay-per-call** in **USDC/RLUSD**. No human API keys. No monthly key babysitting.

## Quick Start (30 seconds)

```bash
# free demo against live host (no wallet required)
npx --yes node -e "fetch('https://mcp-x402.onrender.com/health').then(r=>r.json()).then(console.log)"

# or clone/run package demo
npm run demo
```

Add to Claude Code / Cursor config (remote — easiest):

```json
{
  "mcpServers": {
    "sml": {
      "url": "https://mcp-x402.onrender.com/mcp",
      "transport": "streamable-http"
    }
  }
}
```

Local:

```bash
npx @scriptmasterlabs/mcp-x402
```

## Hooks for agents & directories

- MCP server for agents with autonomous USDC/RLUSD payments
- x402 pay-per-call
- 85+ trading + federal tools
- no API keys
- free discover / tools/list path
- ACP wedges: gas_tracker $0.001 · rwa_intelligence $0.001 on scriptmasterlabs

## Live

- Remote MCP: https://mcp-x402.onrender.com/mcp
- Health: https://mcp-x402.onrender.com/health
- OpenAPI x402: https://mcp-x402.onrender.com/.well-known/x402
- Agent Economy OS: https://www.scriptmasterlabs.com/agent-economy-os
- npm: https://www.npmjs.com/package/@scriptmasterlabs/mcp-x402

## Pricing posture

- Free: health, discover/list, selected demo tools
- Paid: micro USDC/RLUSD per tool via x402 (agent pays mid-loop)
- ACP marketplace: gas_tracker $0.001 trial wedge


<!-- see README.md for full tool list and payment flow -->

## Housing / Section 8 ($0.001)
- /x402/pha-lookup · /x402/hcv-fmr · /x402/hud-vash-contacts
- /x402/housing-landlord-checklist · /x402/housing-windsor

## Paid snack URLs (HTTP 402)
- https://mcp-x402.onrender.com/x402/crypto-price
- https://mcp-x402.onrender.com/x402/fx-rate
- https://mcp-x402.onrender.com/x402/grants
Root `/` is discovery catalog only. Point scanners at paid snacks above.

## EchoNet (this host)
- GET /x402/bounties (free board)
- GET /x402/echonet (manifest)
- GET /x402/hunters (teaser)
- GET /x402/graph?task=llm-chat (paid 0.001)
- POST /x402/hunters/report (paid 0.001)
- POST /x402/bounties (paid listing)
- www: https://www.scriptmasterlabs.com/echonet.html



## Payment rails
- accepts[0]: Base USDC eip155:8453 · X-PAYMENT (CDP/facilitator) or X-PAYMENT-TX (sovereign)
- accepts[1]: Robinhood Chain USDG eip155:4663 · X-PAYMENT-TX sovereign (asset 0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168)
- Discovery: GET /.well-known/x402 · GET /x402/facilitators · list on agent402.tools/sell


## Agent physiology & markets (new)
- Sleep MCP (ASP): POST /x402/sleep/cycle · GET /x402/sleep/dreams · POST /x402/sleep/nightmare · GET /x402/sleep/health
- AFX Attention Futures: GET /x402/afx/quote · POST /x402/afx/buy-block · POST /x402/afx/sell-distraction · POST /x402/afx/deep-focus · GET /x402/afx/portfolio · GET /x402/afx/health
- Sacred/Profane Boundary: POST /x402/sacred/enshrine · GET /x402/sacred/verify · POST /x402/profane/discard · GET /x402/sacred/health
- All paid snacks $0.001 — Base USDC or Robinhood USDG (X-PAYMENT / X-PAYMENT-TX)


## Novel Package — NEGATIVE_SPACE_14
14 outside-the-box agent infrastructure concepts as one product line.
- FREE catalog: GET /x402/novel · GET /x402/novel/catalog · GET /x402/novel/manifest · GET /x402/novel/health
- LIVE (3/14): Sleep MCP · AFX Attention Futures · Sacred/Profane Boundary
- Bundle snack: POST /x402/novel/bundle { agent_id, depth?, attention_ms?, enshrine_object_id?, enshrine_hash? } — $0.001
- Roadmap 11: EaaS, RMP, Synchronicity, Thanatology, Friction, Taboo, Unlearning, NCP, Shadow, Phase, Mimicry

### #6 Cognitive Friction (LIVE)
- GET /x402/friction/health (free)
- POST /x402/friction/apply { agent_id, action, intensity?, amount_usd? }
- POST /x402/friction/explain { session_id, explanation }
- GET /x402/friction/status?session_id=
- POST /x402/friction/confirm { session_id, reconfirm_token, accept_risk: true }

## Attention Broker (LIVE) — crawler-of-crawlers (Redis-durable job board)
Pay indexes/routers for **verified** attention (re-index, canary), not vibes.
- FREE: GET /x402/attention/health · GET /x402/attention/discover · GET /x402/attention/bounties
- PAID $0.001: POST /x402/attention/bounty { sponsor_origin }
- PAID: POST /x402/attention/claim { bounty_id, crawler_id, proof: { challenge } }
- PAID: POST /x402/attention/verify { bounty_id } — broker re-fetches /.well-known/x402

## Payment rails (LIVE)
1. Base USDC — eip155:8453 — payTo EVM 0x7233… — X-PAYMENT or X-PAYMENT-TX
2. Robinhood USDG — eip155:4663 — payTo EVM 0x7233… — X-PAYMENT-TX
3. Solana USDC — solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp — payTo E4d3Jwc… — X-PAYMENT-TX=<base58 sig>
