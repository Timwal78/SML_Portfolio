name: keepalive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping mcp-x402
        run: |
          curl -sf https://mcp-x402.onrender.com/health || echo "mcp-x402 health check failed"
